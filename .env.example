# Optional. Socrata rate-limits anonymous callers by IP; a free app token from
# https://opendata.rdw.nl/profile/edit/developer_settings lifts that limit.
# The token identifies the application, not a user, and is sent from the
# browser - it is not a secret, but keep it out of version control anyway.
VITE_RDW_APP_TOKEN=
